namespace Game
{
    public enum UIViewCode
    {
        Undefined = 0,

        // CORE
        Core = 10,
        ConstructionsOverlay = 20,
        OutpostsOverlay = 30,
        PlayerSquadOverlay = 40
    }
}
